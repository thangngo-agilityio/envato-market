---
// Components
import { ProductCard } from '@app/components';

// Constants
import { ROUTES } from '@app/constants';

// Mocks
import { PRODUCTS } from '@app/mocks';

const { id, name, amount, currency, imageURLs } = PRODUCTS[0];
---

<ProductCard
  href={`${ROUTES.PRODUCTS}/${id}`}
  name={name}
  amount={amount}
  currency={currency}
  imageURL={imageURLs[0]}
/>
